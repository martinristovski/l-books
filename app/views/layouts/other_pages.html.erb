<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>L'Books</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'style', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700;0,800;1,300;1,400;1,500;1,600;1,700;1,800&family=Parisienne&display=swap" rel="stylesheet">
  </head>

  <body>
    <div id="topbar_new">
      <%= link_to "L'Books", '/', :id => 'navbar-logo' %>
      <% if session[:user_id].nil? %>
        <%= link_to "Sign In", '/signin', :id => 'signin' %>
        <%= link_to "Register", '/signup', :id => 'signin' %>
      <% else %>
        <%= link_to "Sign Out", '/logout', :id => 'signin' %>
        <%= link_to "New Listing", '/listing/new', :id => 'signin' %>
      <% end %>
    </div>
    <% if flash[:notice] %>
      <div class="alert alert-info" role="alert" id="notice">
        <%= flash[:notice] %>
      </div>
    <% elsif flash[:warning] %>
      <div class="alert alert-danger" role="alert" id="warning">
        <%= flash[:warning] %>
      </div>
    <% end %>
    <%= yield %>
  </body>
</html>
